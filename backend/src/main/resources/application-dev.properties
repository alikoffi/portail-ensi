# =====================================================================
# Profil DEV : ajoute les donnees de demonstration (flyway/dev).
# A NE PAS activer en production.
# =====================================================================

# Flyway : structure + seed de demonstration
spring.flyway.locations=classpath:flyway/schema,classpath:flyway/dev

# Logs SQL plus verbeux en dev
spring.jpa.show-sql=true
